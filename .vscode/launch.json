{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI: Run Server",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run_server.py",
            "jinja": true,
        }
    ]
}
